mixin header(active)
	nav.navbar.navbar-inverse
		.navbar-inner
			.container-fluid
				.navbar-header
					a.btn.btn-navbar(data-toggle='collapse' data-target='.nav-collapse')
						span.icon-bar
						span.icon-bar
						span.icon-bar
					a.navbar-brand(href='/') e i g i e
				.navbar-collapse.collapse
					ul.nav.navbar-nav
						li.dropdown(class=active==="local" ? "active" : "")
							a(href="#", class="dropdown-toggle" data-toggle="dropdown") 
								span.glyphicon.glyphicon-user
								&nbsp;Local Scan &nbsp
								b.caret
							ul.wider-dropdown.dropdown-menu
								li This will take the names in local and summarize them.  Select the name list in local, hit control-a, control-c to copy them all to clipboard. Then control-v in the below area.
								li.divider
								li
									form(action="/local_scan?section=local" method="post")
										textarea.form-control(rows=10, name="scan",width="95%")
										p
										button(type="submit", class="btn btn-primary") Scan Local
						li.dropdown(class=active==="dscan" ? "active" : "")
							a(href="#", class="dropdown-toggle" data-toggle="dropdown") 
								span.glyphicon.glyphicon-screenshot
								&nbsp;D Scan &nbsp
								b.caret
							ul.wider-dropdown.dropdown-menu
								li This will take the output of a dscan and summarize it.  Select the dscan table, hit control-a, control-c to copy them all to clipboard. Then control-v in the below area.
								li.divider
								li
									form(action="/d_scan?section=dscan" method="post")
										textarea.form-control(rows=10, name="dscan",width="95%")
										p
										button(type="submit", class="btn btn-primary") Analyze D Scan

									
