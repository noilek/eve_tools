mixin header(active)
	nav.navbar.navbar-inverse
		.navbar-inner
			.container-fluid
				.navbar-header
					a.btn.btn-navbar(data-toggle='collapse' data-target='.nav-collapse')
						span.icon-bar
						span.icon-bar
						span.icon-bar
					a.navbar-brand(href='#') EIGIE
				.navbar-collapse.collapse
					ul.nav.navbar-nav
						li(class=active === "local" ? "active" : "")
							a(href="/?section=local") 
								span.glyphicon.glyphicon-user
								&nbsp Local Scan Tool
